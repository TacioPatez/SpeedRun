import React from 'react'
import { StyleSheet } from 'react-native'

export default StyleSheet.create({ // o '{' é pra indicar que é um objeto
    txtGrande: {
        fontSize: 24,
        color: 'black',
        textAlign: 'center'
    }
})